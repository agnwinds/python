name: Central_object.rad_type_for_star_to_make_wind
description: |
  The way in which radiation from the central object will be simulated, either as a bb or from models which
  have been read in separately.
type: Enum (Int)
values:
  0: bb
  1: models
parent:
  parameter: Condition e.g. >0 or list e.g. [1, 2, 5]
file: setup.c
