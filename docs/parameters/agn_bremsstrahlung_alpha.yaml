name: agn_bremsstrahlung_alpha
description: |
  The frequency exponent alpha in bremstrahlung SED of the form
  L_nu=nu**alpha exp(-hnu/kT)
type: Double
unit: None
values: Any - sign is not asssumed so use negative if you want negative
parent:
  parameter: Condition e.g. greater than 0 or list e.g. [1, 2, 5]
file: setup.c
