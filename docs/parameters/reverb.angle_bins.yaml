name: reverb.angle_bins
description: |
  Used when generating 3d .vtk output files for visualisation. Sets the number
  of angle bins used in the output. Aesthetic only; bigger makes prettier meshes
  with larger filesizes.
type: Int
values: >0
default: 100
parent:
  reverb.mode: [1, 3]
file: setup_reverb.c
