name: spec.type
description: |
  Multi-line description, must keep indentation.
type: Enum (Int)
values:
  other: basic
  1: flambda
  2: fnu
parent:
  parameter: Condition e.g. >0 or list e.g. [1, 2, 5]
file: setup2.c
